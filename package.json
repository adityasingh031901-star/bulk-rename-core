{
	"name": "bulk-rename-core",
	"version": "0.1.0",
	"description": "Local-first batch filename logic used by Bulk File Rename — no network, no analytics, no secrets.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"license": "MIT",
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"typescript": "^5.5.0"
	}
}
